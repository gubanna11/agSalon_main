@model IEnumerable<Worker>

@{
    ViewData["Title"] = "Workers";
}

<div>
    @foreach (var item in Model)
    {
        <p>@item.Surname</p>
        foreach (var wg in item.Workers_Groups)
        {
            <p>@wg.Group.Name</p>
        }
        <br /><br />
    }
</div>